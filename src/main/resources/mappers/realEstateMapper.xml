<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.devwork.springtest.mybatis.repository.RealEstateRepository">
    <select id="selectRealEstate" parameterType="int"
    resultType="com.devwork.springtest.mybatis.domain.RealEstate">
        SELECT
            `id`
            , `realtor_id`
            , `address`
            , `area`
            , `type`
            , `price`
            , `rent_price`
            , `created_at`
            , `updated_at`
        FROM
            `real_estate`
        WHERE
            `id` = #{id}
    </select>

    <select id="selectRowRentPrice" parameterType="int"
    resultType="com.devwork.springtest.mybatis.domain.RealEstate">
        SELECT
            `id`
             , `realtor_id`
             , `address`
             , `area`
             , `type`
             , `price`
             , `rent_price`
             , `created_at`
             , `updated_at`
        FROM
            `real_estate`
        WHERE
            <![CDATA[
            `type` = "월세"
        AND
            `rent_price` < #{rent}
            ]]>
    </select>

    <select id="selectRowPrice" parameterType="int"
    resultType="com.devwork.springtest.mybatis.domain.RealEstate">
        SELECT
            `id`
             , `realtor_id`
             , `address`
             , `area`
             , `type`
             , `price`
             , `rent_price`
             , `created_at`
             , `updated_at`
        FROM
            `real_estate`
        WHERE
            <![CDATA[
            `area` >= #{area}
        AND
            `price` <= #{price}
            ]]>
        ORDER BY
            `created_at` DESC
    </select>

    <insert id="insertRealEstate" parameterType="com.devwork.springtest.mybatis.domain.RealEstate">
        INSERT INTO
            `real_estate`
        (
            `realtor_id`
            , `address`
            , `area`
            , `type`
            , `price`
        )
        VALUE
        (
            #{realtorId}
            , #{address}
            , #{area}
            , #{type}
            , #{price}
        )
    </insert>

    <insert id="paramInsertRealEstate" parameterType="map">
        INSERT INTO
            `real_estate`
        (
          `realtor_id`
        , `address`
        , `area`
        , `type`
        , `price`
        , `rent_price`
        )
            VALUE
            (
            #{realtorId}
            , #{address}
            , #{area}
            , #{type}
            , #{price}
            , #{rentPrice}
            )
    </insert>

    <update id="updateRealEstateById" parameterType="map">
        UPDATE
            `real_estate`
        SET
            `type` = #{type}
             , `price` = #{price}
        WHERE
            `id` = #{id}
    </update>

    <delete id="deleteRealEstateById" parameterType="int">
        DELETE
        FROM
            `real_estate`
        WHERE
            `id` = #{id}
    </delete>
</mapper>